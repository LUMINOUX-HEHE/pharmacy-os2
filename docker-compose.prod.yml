services:
  api:
    restart: always
    environment:
      NODE_ENV: production

  web:
    restart: always

  nginx:
    restart: always
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot_data:/etc/letsencrypt

volumes:
  certbot_data:
